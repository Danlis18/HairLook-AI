const form=document.querySelector('#signinForm'),result=document.querySelector('#signinResult'),error=document.querySelector('#signinError');
const params=new URLSearchParams(location.search);if(params.get('error'))error.textContent='That sign-in link is expired or has already been used. Request a new one below.';
form.addEventListener('submit',async e=>{e.preventDefault();error.textContent='';result.textContent='';const btn=form.querySelector('button');btn.disabled=true;btn.textContent='Sending…';try{const r=await fetch('/api/auth/request-link',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:form.email.value.trim()})});const data=await r.json();if(!r.ok)throw new Error();result.textContent=data.message||'Check your email for a secure sign-in link.';if(data.devMagicLink){const a=document.createElement('a');a.href=data.devMagicLink;a.textContent=' Open demo sign-in link';a.style.textDecoration='underline';a.style.marginLeft='4px';result.appendChild(a);}form.reset();}catch{error.textContent='Could not send the sign-in link. Please try again.';}finally{btn.disabled=false;btn.textContent='Send secure sign-in link →';}});
